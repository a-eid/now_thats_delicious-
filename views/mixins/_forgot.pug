mixin forgotForm()
  form.form(action="/account/forgot" method="POST" autocomplete="off")
    h2 I forgot my password 
    label(for="email") Email 
    input#email(type="email" name="email")
    input.button(type="submit" value="Send a Reset")